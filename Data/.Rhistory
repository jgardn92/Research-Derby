install.packages("lubridate")
install.packages("chron")
require(lubridate)
require(chron)
setwd("G:/Team Drives/RD-252/Data")
smoke_days <- read.csv("smoke_template.csv",header = TRUE)
View(smoke_days)
as.poSIXct(smoke_days$Date,format = "%m/%d/%Y")
as.POSIXct(smoke_days$Date,format = "%m/%d/%Y")
dates <- as.POSIXct(smoke_days$Date,format = "%m/%d/%Y")
dates <- data.frame(as.POSIXct(smoke_days$Date,format = "%m/%d/%Y"))
separate(dates, "date", c("Year", "Month", "Day"), sep = "-")
View(dates)
ymd(dates)
data.frame(as.POSIXct(smoke_days$Date,format = "%m/%d/%Y"))
dates <- as.Date(as.POSIXct(smoke_days$Date,format = "%m/%d/%Y"))
yday(dates)
year(dates)
year <- year(dates)
julian_day <- yday(dates)
smoke_days2 <- cbind(smoke_days,julian_day, year)
View(smoke_days2)
smoke_days2 <- smoke_days2[,2:5]
View(smoke_days2)
saveRDS(smoke_days2,"smoke_days.RDS")
